import { Illuminate } from "@/components/illuminate"

/**
 * v2.5.0 - Admin Portal Enhanced with Company Filtering & Data Management
 * ✓ Company Filter Dropdown - dynamically populated with unique companies from responses
 * ✓ Smart CSV Export - exports only filtered data with dynamic filename (company-specific if filter applied)
 * ✓ Individual Row Deletion - trash icon per row with confirmation dialog and instant table refresh
 * ✓ Actions column added to responses table for better data management
 */
export default function Page() {
  return <Illuminate />
}
